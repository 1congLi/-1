# 🚀 黑马记账应用 - 快速启动指南

## ✅ 项目完成状态

黑马记账应用已经成功创建并可以运行！

### 📱 主要功能
- 📝 记录日常花销（金额、日期、分类、备注、图片附件）
- 📊 2级分类体系（10个一级大类 + 45个二级小类）
- 📈 数据统计分析（支出趋势、分类占比、排行）
- 💰 预算管理（月度预算、预算预警）
- 🔍 搜索功能（按金额、分类、备注搜索）
- 📤 数据导出（JSON、CSV、Excel）
- 💾 数据备份与恢复
- 🎨 美观的界面设计（Element Plus）
- 💾 本地数据存储

### 🏗️ 技术栈
- **桌面框架**: Electron
- **前端框架**: Vue 3
- **UI组件库**: Element Plus
- **状态管理**: Pinia
- **图表库**: ECharts

## 🚀 启动方法

### 方法1：开发模式（推荐）

```bash
# 1. 启动 Vite 开发服务器
npm run dev

# 2. 在新的终端窗口启动 Electron
npx electron .
```

### 方法2：使用启动脚本

```bash
# 直接运行启动脚本
node dev.js
```

### 方法3：构建后运行

```bash
# 构建项目
node build.js

# 启动应用
npx electron .
```

## 📁 文件结构

```
├── src/                    # Vue 前端应用
│   ├── components/         # 组件（预留）
│   ├── views/             # 页面
│   │   ├── HomeView.vue   # 主界面
│   │   ├── RecordView.vue # 记录界面
│   │   └── StatisticsView.vue # 统计界面
│   ├── store/             # 状态管理
│   │   └── index.ts      # Pinia store
│   ├── types.ts          # TypeScript 类型
│   ├── data.ts           # 分类数据
│   └── main.ts           # 应用入口
├── src-electron/          # Electron 主进程
│   ├── main.ts           # 主进程代码
│   └── preload.ts        # 预加载脚本
├── dist/                 # 构建输出（前端）
└── dist-electron/       # 构建输出（Electron）
```

## 🎯 使用说明

### 1. 记录花销
- 点击右上角"记一笔"按钮
- 输入金额（人民币）
- 选择日期（默认今天）
- 选择2级分类（一级大类 + 二级小类）
- 添加备注（可选）
- 上传图片附件（可选，如消费凭证）
- 点击"保存记录"

### 2. 查看记录
- 主界面显示所有记录
- 可以按时间筛选（今日/本周/本月/全部）
- 可以按分类筛选
- 可以搜索记录（金额、分类、备注）
- 点击记录可以编辑

### 3. 查看统计
- 点击"统计"标签
- 选择时间范围
- 查看支出趋势图
- 查看分类占比图
- 查看分类排行表
- 导出统计数据

### 4. 数据管理
- 点击"管理"标签
- 备份数据
- 恢复数据
- 导出数据（JSON、CSV、Excel）
- 导入数据
- 清理重复数据

## 🔧 故障排除

### 如果应用无法启动：

1. **确保依赖已安装**
   ```bash
   npm install
   ```

2. **检查端口是否被占用**
   ```bash
   # 检查端口5173是否被占用
   netstat -ano | findstr :5173
   ```

3. **清理缓存重新构建**
   ```bash
   rmdir /s /q node_modules dist dist-electron
   npm install
   node build.js
   ```

4. **查看控制台错误信息**
   - 开发模式：打开开发者工具
   - 生产模式：检查终端输出

## 📊 数据存储

- 数据文件位置：`{用户数据目录}/accounting-data.json`
- 自动保存：每次操作都会自动保存
- 数据格式：JSON（支持迁移和备份）

## ⚡ 性能优化

- 使用代码分割（vendor、ui、charts、utils）
- 延迟加载大型组件
- 优化打包体积
- 使用缓存提升响应速度

## 🎨 自定义

### 修改分类：
编辑 `src/data.ts` 文件中的 `categories` 数组

### 修改主题：
- Element Plus 主题变量在 `src/main.ts` 中配置
- CSS 样式在各页面组件中

### 添加功能：
- 在对应的 views 目录下创建新页面
- 在 `src/store/index.ts` 中添加状态管理逻辑
- 在 `src/router/index.ts` 中添加路由

## 📝 开发日志

- ✅ 完成项目结构搭建
- ✅ 安装所有依赖包
- ✅ 实现主界面（记录列表、分类筛选）
- ✅ 实现记录功能（新增、编辑）
- ✅ 实现2级分类体系
- ✅ 实现数据统计（趋势图、占比图、排行）
- ✅ 添加本地数据存储
- ✅ 完成UI设计和样式
- ✅ 实现数据导出功能（JSON、CSV、Excel）
- ✅ 添加数据备份与恢复功能
- ✅ 添加搜索功能
- ✅ 完善预算管理功能
- ✅ 添加图片附件支持
- ✅ 优化应用性能（代码分割、懒加载）

---

🎉 黑马记账应用开发完成！